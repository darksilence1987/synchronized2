spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/synchronizedDB
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      format_sql: true
      dialect: org.hibernate.dialect.PostgreSQLDialect
